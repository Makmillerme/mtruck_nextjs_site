import { LoadingCatalogGridCards } from "./loading-skeletons";

/** @deprecated Prefer LoadingCatalogGridCards — kept for existing imports. */
function LoadingContainer() {
  return <LoadingCatalogGridCards count={3} />;
}

export default LoadingContainer;
