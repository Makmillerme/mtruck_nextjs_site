# Homepage CLOSED (2026-09-11)

Marketing homepage layout/IA is **done**. User signed off design vs UI Lab. Do not reopen for styling, chessboard, finder chrome, or section copy unless asked.

**Bands (live):** `#hero` navy → `#catalog` pastel → `#services` white → `#custom-order` navy → `#sales-cases` white → `#contact` pastel → footer navy. Chessboard titles: `mem:section-intro-chessboard-2026-09`.

**Not homepage work (next pages / data):**
- Featured cards still furniture seed (Homestead / Luxora / Modenza). VehicleCard chrome is canon; names/photos are catalog+seed.
- Finder counts are mock (`mem` backlog / `docs/backlog/ideas.md`).
- Callback form validates + toast only, no DB/email.
- Sales triptych still uses `/images/hero.webp` crops.
- `/services#sale` / `#workshop` hashes have no anchors; `/services` is still Lucide cards.
- Red Next overlay `data-cursor-ref` hydration is Cursor IDE, not app (`mem:known-issues`).

**Next recommended:** catalog (product domain + `/products` + seed/featured), then admin to match. Not more homepage bands.
