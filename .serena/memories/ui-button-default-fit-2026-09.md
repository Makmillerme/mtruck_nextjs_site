# Default Button vs brand (2026-09-09)

User picked **01+03**. Now live:
- `--primary: 222 47% 11%` (same as foreground). Electric `#0047AB` retired.
- `[data-header-surface="dark"]` inverts `--primary` to white (hero, custom-order, partnership, footer, UI Lab navy).
- Homepage checked: hero CTA white plate; contact/card navy fill; custom-order white plate.
- UI Lab Button section is the canonical preview. CTA Lab is a pointer only.
- `destructive` mirrors `default` (solid fill, transparent border for size parity with outline). Account «Вийти» = `destructive` sm, not outline+red text.
