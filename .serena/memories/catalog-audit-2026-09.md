# Catalog page audit (2026-09)

- Manrope site-wide (see typography-manrope.mdc)
- Grid/list toggle: hidden below md; desktop `useState` + `buildCatalogHref` + cookie (no history.replaceState+refresh)
- List view = `VehicleCard layout="list"`
- H1 Каталог + `.page-content`
- Filter generated from CMS isFacet / showInFilter
