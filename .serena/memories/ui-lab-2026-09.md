# UI Lab (2026-09)

- Route: `/ui-lab` (dev). Sticky search + category chips. Header canon is section `header` (category Бренд) in `components/dev/ui-lab-header.tsx`.
- Header mocks use the same `.site-header-glass` + `.site-header` split as production: photo / navy / white / pastel. Do not wrap mocks in `overflow-hidden` (kills blur). Do not put blur on the `<header>` with buttons.
- Other sections: logo, icons, buttons, fields, marks, overlays, cards (VehicleCard as children).
- Filter meta: `components/dev/ui-lab-catalog.ts`.
