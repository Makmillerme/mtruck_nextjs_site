# UI Lab (2026-09-10)

- Route: `/ui-lab` (dev). Sticky search + category chips.
- Header canon: section `header` in `components/dev/ui-lab-header.tsx`.
- Homepage close: section `homepage-close` (`components/dev/ui-lab-homepage-close.tsx`) — mock sales plates, compact CTA, footer fragment on navy. Filter meta in `components/dev/ui-lab-catalog.ts`.
- Header mocks use `.site-header-glass` + `.site-header`. Do not wrap mocks in `overflow-hidden` (kills blur).
- Other sections: logo, icons, buttons, fields, marks, overlays, cards.
