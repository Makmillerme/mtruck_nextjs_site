# UI Lab canon notes (2026-09-10)

- Live `CategoryFinderPanel` under section `finder`.
- Homepage-close includes CustomOrder CTA (white plate, centered) before sales cases.
- VehicleCard: shadow + zoom on hover; not the same rule as shadowless buttons.
- Button navy: document CustomOrder as centered `default`, not ghost.
- Finder chips: active navy fill, idle `bg-secondary`.
