# UI Lab canon notes (2026-09-10, updated 2026-09-17)

- Live `CategoryFinderPanel` under section `finder`.
- Homepage-close includes CustomOrder CTA (white plate, centered) before sales cases. Sales mock header is `align=end` (title right), triptych keeps 1.05 / 1.3 / 1.05.
- VehicleCard: shadow + zoom on hover; not the same rule as shadowless buttons.
- Button navy: document CustomOrder as centered `default`, not ghost.
- Finder chips: active navy fill, idle `bg-secondary`.
- Access: `app/[locale]/ui-lab/layout.tsx` — `NODE_ENV === production` → `notFound()`; otherwise `getAdminUser()`. Proxy `protectedPaths` includes `/ui-lab` (guest → sign-in). Covers `/ui-lab` and `/ui-lab/cta`.
- **Delete confirm:** overlays section includes AlertDialog demo + rule. See `ui-confirm-delete-2026-09`.
- **Layout rules (2026-09-17):** category «Розмітка», section `layout` → `components/dev/ui-lab-layout.tsx`. Documents font (Plus Jakarta / Geist Mono), `.page-container` / `.page-content` gutters, `.section-spacing` / tight, full-bleed nesting. Source of truth remains `app/globals.css` + `layout-page-container-gutters-2026-09`.
