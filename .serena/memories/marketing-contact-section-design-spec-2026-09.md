# Contact section (2026-09-09)

`components/contact-section.tsx` + `components/contact-form.tsx`.

- Homepage: after Services, `full-bleed`, `id="contact"`, **`bg-secondary`** (pastel wash, same as catalog). No `border-t` — color change is the break.
- Also used on `/contact` (same pastel page + navy footer).
- Info cards + map placeholder: `bg-background` (white on pastel).
- Left: callback form (Zod `callbackInquirySchema`, `submitCallbackInquiryAction`). Privacy → `/privacy`.
- Copy: `ContactSection` uk/en/de. Icons: lucide.
