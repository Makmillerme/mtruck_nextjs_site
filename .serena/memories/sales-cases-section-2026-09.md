# Sales cases (2026-09-10)

Homepage section after CustomOrder: `components/sales-cases-section.tsx`, `#sales-cases`, `bg-background` (white).

- Role: proof — who we already delivered. Anonymized buyer = business type + city. No real names.
- Layout: stepped diagonal plates (offset down-right, slight `rotate` on `lg+`). Mobile stacks, no tilt.
- Captions: route `font-mono`, model `font-black`, buyer `text-muted-foreground`. No icon circles.
- Photos: placeholders from `/images/hero.webp` with different `object-position` until real case shots exist.
- i18n: `SalesCases` in uk/en/de.
- Do not wire `components/trust-strip.tsx`.
