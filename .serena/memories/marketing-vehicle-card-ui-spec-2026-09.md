# VehicleCard — 01+03 + motion

Date: 2026-09-10

- `rounded-sm`, `border-border`, white plate.
- **Keep** `shadow-sm` + `group-hover:shadow-xl` (`duration-500`). Buttons have no drop-shadow; **cards do**.
- Photo: `object-cover transition-transform duration-500 group-hover:scale-105`.
- Icons: `react-icons/lu` (`LuRoute`, `LuSettings2`, `LuLeaf`, `LuShieldCheck`, `LuChevronRight`).
- UI Lab vehicle block is the live RSC `VehicleCard` — no separate mock. Hint documents shadow + zoom.
