# Session 2026-09-18 catalog filter UX

Simplified public filter after user rejected checkboxes + extra chevron.
Open leaf = `folder=` that slug. Primary buttons, indent, 200ms expand. SELECT facets depend on CMS `dependsOnAttributeId` / `parentOptionId` (model grouped by make).
