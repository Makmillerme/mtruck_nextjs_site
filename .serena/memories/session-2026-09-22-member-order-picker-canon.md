## MemberOrderPicker sheet canon (2026-09-22)

Replaced native `<select>` in `MemberOrderPicker` (`display-groups-panel.tsx`) with `SearchableEntityPicker` (UI Lab sheet combobox: outline Button h-11 + Popover + cmdk). Selecting an attribute immediately adds it to the ordered list.

Used by product name settings dialog and CMS display groups sheets.
