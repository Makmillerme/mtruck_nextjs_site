# CustomOrder section

Date: 2026-09-10

- Navy `data-header-surface="dark"` band.
- 12-col header, mono eyebrow, `font-black` title, four ledger steps (01–04 + Lu icons).
- CTA: **`Button size="lg"` `default`**, **centered**. On dark this is the white plate. Never ghost, never `px-0` left-aligned text.
- Links to `#contact`.
- UI Lab: homepage-close navy mock + button-surfaces navy `custom-order · default по центру`.
