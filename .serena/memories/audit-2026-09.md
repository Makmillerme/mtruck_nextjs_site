# Audit fixes 2026-09-09

## Done
- Header flicker: removed client `useSession` swap; avatar `priority`; deleted HtmlLang.
- Hero LCP: hero.webp + ReactDOM.preload; removed unused hero.jpg / product-big.jpg.
- Catalog: Suspense stream, active layout only, batch favorites, productListSelect, priority first 3 cards, error/loading boundaries, CategoryFinder ready via useSyncExternalStore.
- Security: `UserRole` enum + `role` on User; Better-Auth additionalFields; `getAdminUser` by role; admin layout gate; create/image product actions require ADMIN + ownership on image; server File validation; no NEXT_PUBLIC_ADMIN_*.
- Correctness: locale redirects, review dedupe + optional authorImageUrl, renderError → Actions.error, test accounts only in development, ShareButton origin fallback, /contacts → /contact.
- Cleanup: removed unused CartButton, NavSearch, UserIcon, SignOutLink, HeroCarousel, ProductCard, navbar-01 Space block, unused `links` export.

## Backlog (docs/backlog/ideas.md)
- Cart toggle per category; persist inquiries; real CategoryFinder counts; live order flow.

## Note
- `npm run dev` was briefly stopped to unlock Prisma generate during role migration; restart locally if needed.
