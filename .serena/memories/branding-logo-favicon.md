Branding assets (2026-09-08):
- Header logo: `components/navbar/Logo.tsx` native `<img>` `/logo_mtruck.svg` (`h-8` / `sm:h-9` / `lg:h-10`). Favicon: `public/favicon_mtruck.svg` + `app/icon.svg`.
- Flat metallic: one shared linear steel fill (`#C8CAD0` → `#8E9096` → `#55575C` → `#2F3136`). No bevel, no `feDropShadow`, no Figma inner-shadow stacks. User rejected 3D volume.
