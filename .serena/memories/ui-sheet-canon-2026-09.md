Sheet canon 2026-09-14: primitive `components/ui/sheet.tsx` is `flex h-full flex-col gap-6 overflow-y-auto p-6 pt-8`, sides `w-full sm:max-w-lg`, header text-left, title text-xl tracking-tight. Do not override width/padding on callers except sidebar.tsx (p-0, hide close). Dropdown/popover/select/tooltip use z-[100] pointer-events-auto. globals.css: `body [data-radix-popper-content-wrapper] { z-index: 100; pointer-events: auto }` because modal Dialog sets body pointer-events none. Sheet dismiss is blocked when pointer is on Radix popper/menu. Cascade trigger is full width (no max-w-sm). UI Lab overlays section includes cascade inside the demo sheet.