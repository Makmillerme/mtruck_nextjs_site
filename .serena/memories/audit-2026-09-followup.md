Catalog Prisma select + ProductListItem live in `utils/product-list.ts`, not `utils/actions.ts`. A "use server" file may only export async functions; exporting `productListSelect` object crashed the app.
