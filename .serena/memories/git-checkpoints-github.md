# Git checkpoints + GitHub remote (2026-09-08)

- Rule: `.cursor/rules/git-checkpoints.mdc` (alwaysApply) — local commit after every logical change; restore only with user confirm; no hard reset without YES.
- Repo: https://github.com/Makmillerme/mtruck_nextjs_site (public)
- `origin` → Makmillerme/mtruck_nextjs_site
- Old template remote kept as `upstream-template` (arnobt78 Ecommerce…)
- Baseline checkpoint commit: `30732fc` — chore: checkpoint working MTruck Next.js site state
- `.gitignore` ignores `.cursor/debug-*.log`, `.cursor/tools/`, `.serena/cache/`
