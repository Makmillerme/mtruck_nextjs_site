# Session 2026-08-20 — Clone + Serena activate

## Done
1. Cloned `arnobt78/Ecommerce-Online-Shop-Platform-3--NextJS-Serverless-FullStack` into `D:\Project\mtruck\mtruck_nextjs_site` with `git clone <url> .` so files sit in the workspace root.
2. Called Serena `initial_instructions`, then `activate_project` → project `mtruck_nextjs_site`.
3. Verified: `app`, `components`, `lib`, `prisma`, `public`, `utils`, `package.json` at root. Origin remote points at the GitHub template. Commit `43268c5`.

## Not done
- `npm install` / `npm run dev` (do not start unless user asks)
- Serena onboarding
- Domain adaptation for car sales
- Stack upgrade to Next 16 / Tailwind 4
